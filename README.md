# MiniCash — инструкция по развертыванию проекта

## 📌 Описание проекта
MiniCash — это веб‑приложение для учёта личных финансов, написанное на **Flask + SQLAlchemy + PostgreSQL**, поддерживает категории, транзакции, аналитику и конвертацию валют.

## 🚀 1. Клонирование проекта
```bash
git clone <URL-вашего-репозитория>
cd <папка-проекта>
```

## 🐍 2. Создание виртуального окружения
```bash
python -m venv .venv
source .venv/bin/activate      # для Linux/Mac
.venv\Scripts\activate         # для Windows
```

## 📦 3. Установка зависимостей
```bash
pip install -r requirements.txt
```
Если файла **requirements.txt** нет — его нужно будет создать.

## ⚙️ 4. Настройка переменных окружения
Создайте файл **.env** в корне проекта:
```bash
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=super-secret-key-change-me
DATABASE_URL=postgresql+psycopg2://postgres:<пароль>@localhost:5432/MiniCash
```

## 🗄️ 5. Подготовка базы данных PostgreSQL
1. Создайте базу:
```sql
CREATE DATABASE "MiniCash";
```
2. Проверьте, что логин/пароль совпадают с `DATABASE_URL`.

## 🛠️ 6. Создание таблиц
Проект содержит файл `create_db.py`, запуск которого создаёт структуру БД:
```bash
python create_db.py
```
Если всё прошло успешно, в терминале появится сообщение о создании таблиц.

## ▶️ 7. Запуск приложения
```bash
python app.py
```
Приложение будет доступно по адресу:
```
http://127.0.0.1:5000
```

## 👤 8. Первый запуск
1. Зарегистрируйтесь
2. Укажите начальный баланс
3. Готово — можно пользоваться!

## 🔧 9. Возможные проблемы
- **Ошибка подключения к PostgreSQL** — проверьте `DATABASE_URL`.
- **Модуль не найден** — убедитесь, что активировано виртуальное окружение.
- **Курсы валют не грузятся** — возможна недоступность API ЦБ; приложение продолжит работу.

## 🎯 10. План для продакшена (кратко)
- Использовать Gunicorn + Nginx
- Настроить `.env` на сервере
- Включить безопасный SECRET_KEY
- Использовать HTTPS

---
Если нужно, могу подготовить продакшен‑гайд, Docker‑версию или автоматически сгенерировать `requirements.txt`.

